spring:
  flyway:
    enabled: false
  kafka:
    listener:
      auto-startup: false
  sql:
    init:
      mode: never
  jpa:
    hibernate:
      ddl-auto: none
  neo4j:
    uri: bolt://localhost:7687
    authentication:
      username: neo4j
      password: password
