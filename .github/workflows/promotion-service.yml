name: CI — promotion-service

on:
  push:
    branches: [main, dev, release/**]
    paths: [services/circleguard-promotion-service/**]
  pull_request:
    paths: [services/circleguard-promotion-service/**]

permissions:
  id-token: write
  contents: read

jobs:
  ci:
    uses: ./.github/workflows/_reusable-service-ci.yml
    with:
      service: promotion-service
      build_system: gradle
      context: .
      dockerfile: services/circleguard-promotion-service/Dockerfile
    secrets: inherit
