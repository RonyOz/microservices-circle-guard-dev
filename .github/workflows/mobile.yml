name: CI — mobile

on:
  push:
    branches: [main, dev, release/**]
    paths: [mobile/**]
  pull_request:
    paths: [mobile/**]
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  ci:
    uses: ./.github/workflows/_reusable-service-ci.yml
    with:
      service: mobile
      build_system: npm
      context: mobile
      dockerfile: mobile/Dockerfile
    secrets: inherit
