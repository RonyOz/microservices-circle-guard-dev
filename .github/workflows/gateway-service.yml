name: CI — gateway-service

on:
  push:
    branches: [main, dev, release/**]
    paths: [services/circleguard-gateway-service/**]
  pull_request:
    paths: [services/circleguard-gateway-service/**]
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  ci:
    uses: ./.github/workflows/_reusable-service-ci.yml
    with:
      service: gateway-service
      build_system: gradle
      context: .
      dockerfile: services/circleguard-gateway-service/Dockerfile
    secrets: inherit
