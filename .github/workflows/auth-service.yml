name: CI — auth-service

on:
  push:
    branches: [main, dev, release/**]
    paths: [services/circleguard-auth-service/**]
  pull_request:
    paths: [services/circleguard-auth-service/**]
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  ci:
    uses: ./.github/workflows/_reusable-service-ci.yml
    with:
      service: auth-service
      build_system: gradle
      context: .
      dockerfile: services/circleguard-auth-service/Dockerfile
    secrets: inherit
